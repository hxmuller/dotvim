Install dotvim onto new machine
=======================================================================

Install:

    git clone git://github.com/hxmuller/dotvim.git ~/.vim

Create symlink:

    ln -s ~/.vim/vimrc ~/.vimrc

Switch to the ~/.vim directory, and fetch submodules:

    cd ~/.vim
    git submodule init
    git submodule update


